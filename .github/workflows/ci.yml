# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  #  gitleaks:
  #  uses: NationalLibraryOfNorway/nasa-workflows/.github/workflows/gitleaks.yaml@main
  codeql:
    uses: NationalLibraryOfNorway/nasa-workflows/.github/workflows/codeql.yaml@main
  build:
    uses: NationalLibraryOfNorway/nasa-workflows/.github/workflows/build-mvn-spring-app.yaml

